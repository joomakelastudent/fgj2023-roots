/*
should level be mutable or not?
probably yes. it's easier to handle one state than to handle one state and then the exceptions.
*/


// main game loop
fn main() {
    level = 
    gameloop(level);
}

fn load_level(address) {
    
}

fn gameloop() {
    do {
    println!("Hello, world!");

    //input_state = jotain()
    //

    //update game_state either OOP or fp. whatever makes sense.
    //OOP is faster and better at small scale

    //render(game_state)
    } while (true)
}